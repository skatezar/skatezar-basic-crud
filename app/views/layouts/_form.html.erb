<div class="container">
  <%= simple_form_for(@item) do |f| %>
    <!-- [...] -->
    <%= f.input :category %>
    <%= f.input :brand %>
    <%= f.input :size %>
    <%= f.input :condition %>
    <%= f.input :price %>
    <%= f.input :photos, as: :file, input_html: { multiple: true } %>
    <%= f.submit %>
  <% end %>
 </div>

